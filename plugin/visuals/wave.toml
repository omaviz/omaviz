name = "wave"
label = "Wave"
description = "Smooth sine-wave bars with brightness control."

# NOTE (ADR-0003): the four [[params]] (amplitude/frequency/brightness/peak_fall)
# were inert — the Wave shader branch derives its look from the live spectrum and
# a fixed aesthetic (theme-colored continuous ribbons), reading none of them, and
# the control texture has no free row to wire them without shader + panel changes.
# Wave exposes no user-tunable params in v7.6; only label + description remain.
