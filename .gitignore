# Rust build artifacts (all cargo target trees)
bridge/target/
daemon/target/
engine/target/
archive-v6/**/target/

# local config / runtime
*.omaviz-orig
.DS_Store

# v7+ bundled engine + compiled shader ARE committed (Architecture A: the
# repo root is a self-contained, zero-build drop-in — matches Omarchy's
# plugin model). bin/omaviz-engine and visual.qsb are tracked in git.
# gpu.qsb is regenerated by build.sh (qsb); commit it to keep clones zero-build.
# bin/
