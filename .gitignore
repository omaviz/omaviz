# Rust build artifacts (all cargo target trees)
bridge/target/
daemon/target/
engine/target/
v6/**/target/

# local config / runtime
*.omaviz-orig
.DS_Store

# v7 bundled engine IS committed (Architecture A: the plugin dir is a
# self-contained, zero-build drop-in — matches Omarchy's plugin model).
# Only the intermediate cargo target trees are ignored.
# plugin/bin/
