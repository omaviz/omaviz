# Rust build artifacts (all cargo target trees)
bridge/target/
daemon/target/
engine/target/
archive-v6/**/target/

# local config / runtime
*.omaviz-orig
.DS_Store

# v7 bundled engine + compiled shader ARE committed (Architecture A: the
# plugin dir is a self-contained, zero-build drop-in — matches Omarchy's
# plugin model). plugin/visual.qsb is tracked in git.
# plugin/bin/
